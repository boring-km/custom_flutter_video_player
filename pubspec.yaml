name: custom_video_player
description: 말 그대로 커스터마이징한 Flutter Video Player

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  intl: ^0.17.0
  video_player: ^2.2.7
  flutter_inappwebview:
  logger:
  shared_preferences:
  path_provider:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
  assets:
    - images/
    - html/
